package deque;

import java.util.Comparator;

/* TODO: a type of ArrayDeque, with 2 additional methods and a new constructor. */

public class MaxArrayDeque<T> extends ArrayDeque{

    /* TODO: creates a MaxArrayDeque with the given Comparator. */
    public MaxArrayDeque(Comparator<T> c) {
    }

    /* TODO: returns the maximum element in the deque as governed by the previously given Comparator.
        If the MaxArrayDeque is empty, simply return null */
    public T max() {
        return null;
    }

    /* TODO: returns the maximum element in the deque as governed by the parameter Comparator c.
        If the MaxArrayDeque is empty, simply return null.*/

    public T max(Comparator<T> c) {
        return null;
    }

}
